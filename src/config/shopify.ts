/**
 * Configuración de Shopify (tienda GAB / ba6703).
 * El token se lee de la variable de entorno SHOPIFY_ADMIN_ACCESS_TOKEN.
 */
export const shopifyConfig = {
  storeDomain: 'ba6703.myshopify.com',
  apiVersion: '2026-04',
} as const;
